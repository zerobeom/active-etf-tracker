name: Backfill KoAct holdings (past dates)

on:
  workflow_dispatch:
    inputs:
      start_date:
        description: "시작일 (YYYY-MM-DD, 예: 2025-02-01)"
        required: true
      end_date:
        description: "종료일 (YYYY-MM-DD, 예: 2026-06-26)"
        required: true
      providers:
        description: "특정 운용사/ETF만 (선택, 비워두면 전체) — 예: sol  또는 samsung,time  또는 sol-megatech"
        required: false
        default: ""

permissions:
  contents: write

concurrency:
  group: update-holdings
  cancel-in-progress: false

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Backfill holdings
        run: |
          START="${{ github.event.inputs.start_date }}"
          END="${{ github.event.inputs.end_date }}"
          ONLY="${{ github.event.inputs.providers }}"
          if [ -n "$ONLY" ]; then
            python scripts/fetch_holdings.py "${START//-/}:${END//-/}" "--only=$ONLY"
          else
            python scripts/fetch_holdings.py "${START//-/}:${END//-/}"
          fi

      - name: Commit & push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data
          if git diff --staged --quiet; then
            echo "변동 없음 — 커밋 생략"
          else
            git commit -m "data: backfill ${{ github.event.inputs.start_date }} ~ ${{ github.event.inputs.end_date }}${{ github.event.inputs.providers && format(' ({0})', github.event.inputs.providers) || '' }}"
            git push
          fi
